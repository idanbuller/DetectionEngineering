Results recorded from a real Splunk instance by running `spltest compose` for each
example test and saving the rows the search returned. The unit tests score them, so
the examples stay verified without needing Splunk in CI.
